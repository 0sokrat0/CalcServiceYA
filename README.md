# 🚀 Распределённый вычислитель арифметических выражений

<p align="center">
  <img src="/assets/GOPHER_ACADEMY.png" alt="Gopher Logo" width="200"/>
</p>
<p align="center">
  <img alt="GoLang" src="https://img.shields.io/badge/Go-v1.24-blue?style=flat-square&logo=go"/>
  <img alt="Fiber" src="https://img.shields.io/badge/Fiber-v2.52.6-orange?style=flat-square"/>
</p>
<p align="center">
  <a href="https://t.me/SOKRAT_00">
    <img src="https://img.shields.io/badge/Telegram-sokrat_00-blue?style=for-the-badge&logo=telegram" alt="Telegram"/>
  </a>
</p>

Этот проект представляет распределённый вычислитель арифметических выражений. Система разделена на две основные части:

- **Оркестратор (Server):** Принимает арифметическое выражение от пользователя, разбивает его на последовательные задачи и следит за их выполнением.
- **Агент (Agent):** Получает задачи от оркестратора, выполняет вычисления (каждая операция обрабатывается отдельно с имитацией значительных вычислительных затрат) и отправляет результаты обратно.

> **Примечание:** Для имитации «дорогих» вычислений время выполнения каждой операции задается через переменные среды:
> - `TIME_ADDITION_MS` – время выполнения сложения (мс)
> - `TIME_SUBTRACTION_MS` – время выполнения вычитания (мс)
> - `TIME_MULTIPLICATIONS_MS` – время выполнения умножения (мс)
> - `TIME_DIVISIONS_MS` – время выполнения деления (мс)
>
> Количество параллельных вычислений регулируется переменной среды `COMPUTING_POWER`.

---

## 📖 Содержание

- [Функционал проекта](#-функционал-проекта)
  - [1. Оркестратор (Server)](#1-оркестратор-server)
  - [2. Агент (Agent)](#2-агент-agent)
  - [3. Front-end (опционально)](#3-front-end-опционально)
- [Быстрый старт](#-быстрый-старт)
- [Установка и запуск](#-установка-и-запуск)
  - [1. Подготовка окружения](#1-подготовка-окружения)
  - [2. Запуск Оркестратора (Server)](#2-запуск-оркестратора-server)
  - [3. Запуск Агента (Agent)](#3-запуск-агента-agent)
  - [4. Запуск Front-end (опционально)](#4-запуск-front-end-опционально)
  - [5. Запуск через Docker Compose](#5-запуск-чрез-docker-compose)
  - [Makefile и CI/CD](#makefile-и-cicd)
- [Использование API](#-использование-api)
  - [Создание вычисления выражения](#создание-вычисления-выражения)
  - [Получение списка выражений](#получение-списка-выражений)
  - [Получение конкретного выражения](#получение-конкретного-выражения)
  - [Работа с задачами](#работа-с-задачами)
- [Тестирование](#-тестирование)
  - [Автоматизированные тесты](#автоматизированные-тесты)
  - [Ручное тестирование](#ручное-тестирование)
- [Переменные окружения](#-переменные-окружения)
- [Диаграмма проекта](#диаграмма-проекта)
- [Структура проекта](#-структура-проекта)
- [Контакты](#-контакты)
- [Заключение](#заключение)

---

## 🖥️ Функционал проекта

### 1. Оркестратор (Server)

- **Создание вычисления выражения:**  
  Пользователь отправляет арифметическое выражение (например, `2+2*2`) через HTTP‑POST запрос. Оркестратор:
  - Разбирает строку, строит AST и генерирует набор задач для каждой операции.
  - Сохраняет выражение с уникальным идентификатором (например, `"expression-1"`).
  - Возвращает код **201** при успешном принятии, **422** при невалидных данных и **500** при внутренней ошибке.

- **Получение списка выражений:**  
  Клиент может получить список всех выражений с их статусами (`pending`, `success`, `error`) и итоговыми результатами (если вычисление завершено).  
  Код ответа: **200** (успех) или **500** (ошибка).

- **Получение конкретного выражения:**  
  Позволяет запросить информацию об отдельном выражении по его идентификатору.  
  Коды ответа: **200**, **404** (не найдено) или **500**.

- **Работа с задачами:**  
  Оркестратор предоставляет следующие endpoint-ы:
  - **GET** `/internal/task`: возвращает задачу, готовую к выполнению.
  - **POST** `/internal/task/:id/:result`: принимает результат выполнения задачи, обновляет её и, при необходимости, итоговое выражение.  
  Возможные коды ответа: **200**, **404**, **422**, **500**.

### 2. Агент (Agent)

- **Получение задач:**  
  Агент, работающий в виде демона, постоянно опрашивает оркестратора (через `GET /internal/task`) для получения задач.

- **Параллельная обработка:**  
  Агент запускает несколько горутин (количество определяется переменной `COMPUTING_POWER`), каждая из которых независимо обрабатывает полученные задачи с заданными задержками (заданными переменными `TIME_*_MS`).

- **Отправка результатов:**  
  После выполнения задачи Агент отправляет результат обратно на сервер через `POST /internal/task/:id/:result`, где оркестратор обновляет статус и итоговое значение выражения.

### 3. Front-end (опционально)

Реализован веб-интерфейс, позволяющий пользователю:
- Вводить арифметическое выражение.
- Отправлять запрос без обновления страницы (через HTMX).
- Просматривать динамически обновляемый список выражений и их результаты.

---

## 🚀 Быстрый старт

Если у вас установлен Docker и Docker Compose, достаточно в корне проекта выполнить:

```bash
make build
```

В результате соберутся и запустятся все необходимые контейнеры (Оркестратор, Агент и веб-интерфейс).  
После запуска:
- Откройте [http://127.0.0.1:8080](http://127.0.0.1:8080), чтобы воспользоваться веб-интерфейсом.
- Для проверки работы API воспользуйтесь `curl` или тестовыми запросами из [Postman-сборки](#-использование-api).

---

## 🔧 Установка и запуск

### 1. Подготовка окружения

Убедитесь, что у вас установлены:
- [Go](https://golang.org/doc/install) (рекомендуется версия 1.24 или выше),
- [Git](https://git-scm.com/),
- (Опционально) **Docker** и **Docker Compose** для контейнеризации (используется в Makefile).

### 2. Запуск Оркестратора (Server)

Если хотите запустить Оркестратор локально (без Docker):
```bash
cd orchestrator/cmd/orchestrator
go build -o orchestrator
./orchestrator
```
По умолчанию сервер будет доступен на порту `:8080` (указывается в `config.yaml`).

### 3. Запуск Агента (Agent)

Если хотите запустить Агент локально (без Docker):
```bash
cd agent/cmd/agent
go build -o agent
./agent
```
Агент будет обращаться к Оркестратору по адресу, указанному в его `config.yaml` (по умолчанию `127.0.0.1:8080`).

### 4. Запуск Front-end (опционально)

При запуске Оркестратора локально фронтенд (статические файлы) автоматически раздаётся на том же порту `8080`. Откройте в браузере:

```
http://127.0.0.1:8080
```

### 5. Запуск через Docker Compose

В корне проекта находится файл `docker-compose.yaml`. Для запуска:
```bash
docker-compose up -d --build
```
Это поднимет три контейнера:
1. **orchestrator** (Server)
2. **agent** (Agent)
3. (Опционально) **front-end** — используется те же самые статические файлы, что и локально (встроено в Оркестратор).

После этого приложение будет доступно на порту `8080`.  
Для остановки:
```bash
docker-compose down
```

### Makefile и CI/CD

В проекте присутствует `Makefile`, в котором определены основные команды для сборки, запуска и остановки контейнеров, а также для запуска тестов. Примеры команд:

- `make build` — сборка и запуск контейнеров с помощью Docker Compose (скрывает вывод, отображает анимированный прогресс).
- `make stop` — остановка и удаление контейнеров.

Также можно настроить CI/CD (например, GitHub Actions или GitLab CI), прописав аналогичные шаги по сборке и тестированию в YAML-файлах вашего репозитория.

---

## 📚 Использование API

Существует готовая [Postman-сборка](https://www.postman.com/joint-operations-physicist-90411458/workspace/yacalc/collection/39299728-c97f28c1-8d0f-44ac-a4c2-7b43fabcf3e8?action=share&creator=39299728), в которой собраны тестовые запросы.

Ниже — краткие примеры `curl`-запросов.

### Создание вычисления выражения

**Запрос:**
```bash
curl --location 'http://127.0.0.1:8080/api/v1/calculate' \
  --header 'Content-Type: application/json' \
  --data '{
    "expression": "2+2*2"
  }'
```
**Ответ (пример):**
```json
{
  "id": "expression-1"
}
```

### Получение списка выражений

**Запрос:**
```bash
curl --location 'http://127.0.0.1:8080/api/v1/expressions'
```
**Ответ (пример):**
```json
{
  "expressions": [
    {"id": "expression-1", "rootTaskID": "task-1", "status": "success", "result": 6},
    {"id": "expression-2", "rootTaskID": "task-2", "status": "pending", "result": 0}
  ]
}
```

### Получение конкретного выражения

**Запрос:**
```bash
curl --location 'http://127.0.0.1:8080/api/v1/expressions/expression-1'
```
**Ответ (пример):**
```json
{
  "expression": {"id": "expression-1", "status": "success", "result": 6}
}
```

### Работа с задачами

**Получение задачи для выполнения:**
```bash
curl --location 'http://127.0.0.1:8080/internal/task'
```
**Отправка результата выполнения задачи:**
```bash
curl --location 'http://127.0.0.1:8080/internal/task/task-1/4'
```
*(Пути могут отличаться в зависимости от конкретной реализации.)*

---

## 🧪 Тестирование

### Автоматизированные тесты

Запустите тесты из корня репозитория командой:
```bash
go test ./...
```
Тесты покрывают:
- CRUD‑операции для хранилищ выражений и задач.
- Логику парсинга выражений и генерации задач.
- Корректность работы API оркестратора (создание, получение выражений, обработка задач).

### Ручное тестирование

Используйте приведённые выше примеры `curl`-запросов или Postman для проверки работы API в различных сценариях (валидные данные, ошибки валидации, отсутствие задач, проверка параллельных вычислений и т.д.).

---

## ⚙️ Переменные окружения

Для настройки временных задержек операций и параллельных вычислений используйте следующие переменные (указываются в `orchestrator/config/config.yaml` и `agent/config/config.yaml` или напрямую через окружение):

- `TIME_ADDITION_MS` — время выполнения сложения (мс)
- `TIME_SUBTRACTION_MS` — время выполнения вычитания (мс)
- `TIME_MULTIPLICATIONS_MS` — время выполнения умножения (мс)
- `TIME_DIVISIONS_MS` — время выполнения деления (мс)
- `COMPUTING_POWER` — количество параллельных вычислителей (горутин)

Значения по умолчанию прописаны в соответствующих `config.yaml`. При необходимости их можно переопределить, например:
```bash
export TIME_ADDITION_MS=1000
export COMPUTING_POWER=5
```
Затем запустить Оркестратор/Агент.

---

## Диаграмма проекта

<p align="center">
  <img src="/assets/Diagram.png" alt="Diagram" width="400"/>
</p>

Диаграмма наглядно показывает, как Оркестратор принимает выражения, разбивает их на задачи, а Агент обрабатывает каждую задачу отдельно. После завершения задачи результат возвращается Оркестратору, который обновляет статус выражения.

---

## 📂 Структура проекта

```
.
├── agent
│   ├── cmd
│   │   └── agent
│   │       └── main.go           # Запуск агента
│   ├── config
│   │   ├── config.go             # Загрузка конфигурации для агента
│   │   ├── config.yaml           # Файл конфигурации агента
│   │   └── config_test.go
│   ├── internal
│   │   └── deamon
│   │       ├── deamon.go         # Логика работы демона-агента
│   │       └── deamon_test.go
│   ├── Dockerfile
│   ├── agent                     # Скомпилированный бинарник агента (пример)
│   ├── go.mod
│   └── go.sum
├── assets
│   ├── Diagram.png               # Диаграмма взаимодействия
│   └── GOPHER_ACADEMY.png        # Графические материалы
├── orchestrator
│   ├── cmd
│   │   └── orchestrator
│   │       └── main.go           # Запуск оркестратора
│   ├── config
│   │   ├── config.go             # Загрузка конфигурации для оркестратора
│   │   ├── config.yaml           # Файл конфигурации оркестратора
│   │   └── config_test.go
│   ├── internal
│   │   ├── expr
│   │   │   ├── createExp.go      # Генерация задач из выражения
│   │   │   ├── expr_test.go      # Тесты для парсинга и генерации задач
│   │   │   ├── genTask.go        # Логика генерации задач
│   │   │   ├── parse.go          # Парсинг выражения
│   │   │   └── tokenize.go       # Токенизация выражения
│   │   └── server
│   │       ├── handlers.go       # HTTP-обработчики API
│   │       ├── routes.go         # Настройка маршрутов (Fiber)
│   │       └── server.go         # Основная логика сервера
│   │       └── server_test.go    # Тесты для серверных обработчиков
│   ├── pkg
│   │   ├── GenID
│   │   │   └── generateID.go     # Генерация уникальных идентификаторов
│   │   └── db
│   │       ├── exprStore.go      # Хранилище выражений
│   │       ├── store_test.go     # Тесты хранилища
│   │       └── taskStore.go      # Хранилище задач
│   ├── public                    # Статические файлы для фронтенда
│   │   ├── js
│   │   │   └── template.js       # Шаблонизатор для динамического отображения результатов
│   │   ├── GOPHER_ACADEMY.png    # Альтернативный логотип
│   │   ├── index.html            # HTML-страница веб-интерфейса
│   │   └── main.css              # Стили для фронтенда
│   ├── Dockerfile
│   ├── go.mod
│   └── go.sum
├── Makefile                      # Сценарии сборки и запуска (Docker + анимация прогресса)
├── README.md                     # Документация проекта
└── docker-compose.yaml           # Конфигурация для запуска через Docker Compose
```

---

## 🤝 Контакты

Автор: **Даня (sokrat_00)**  
- Telegram: [@SOKRAT_00](https://t.me/SOKRAT_00)  
- GitHub: [github.com/0sokrat0](https://github.com/0sokrat0)

---
